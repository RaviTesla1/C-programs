main()
{
    int length,breadth;
    int area;
    printf("Enter length and breadth");
    scanf("%d%d",&length,&breadth);
    area =length*breadth;
    printf("Area of rectangle is %d",area);
    getch();
}
